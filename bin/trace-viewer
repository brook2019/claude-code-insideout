#!/usr/bin/env bash
set -euo pipefail

# Interactive Trace Viewer
# Usage: ./bin/trace-viewer <session.jsonl> [-o output.html]

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

exec bun run ./src/tools/traceViewer.ts "$@"
